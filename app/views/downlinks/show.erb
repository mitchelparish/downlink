<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/style.css">
    <title>Downlink established.</title>
  </head>
  <body>
    <div class="main">
      <div class="sub">
        <br><h1><%=@downlink.title%></h1>
        System User: <%=@downlink.user_id%>00090014<br>
        Transmission ID: <%=@downlink.created_at.strftime("%Y-%jT%RZ")%><br>
        Signal Strength: <%=@downlink.signal_strength%>%<br>
        <div class="downlink">
          <%=@downlink.message%>
        </div>
        <% if logged_in? && current_user.id == @downlink.user_id %>
        <a href="/downlinks/<%= @downlink.id %>/edit">Edit Message</a>
        <a href="/users/<%=current_user.slug%>">Control Panel</a>

        <form method="post" action="/downlinks/<%=@downlink.id%>/delete">
          <input id="hidden" type="hidden" name="_method" value="delete">
          <input type="submit" value="Delete Message">
        </form>
        <% end %>
      </div>
    </div>
  </body>
</html>
